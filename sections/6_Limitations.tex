\section*{Limitations}
\label{sec:limitations}

% ============================================================
% OUTLINE -- Limitations    ACL-required, unnumbered; does NOT
% count toward the 4-page limit.   target: ~0.3-0.4 page.
% Be specific and honest -- reviewers reward candor.
% Points to cover:
%   - Results are partial / in progress at submission time;
%     state exactly which runs are complete.
%   - The recall@L co-optimality holds exactly only at equal
%     weights (w+ = w- = 1); the asymmetric-weight setting used
%     in practice is a heuristic, outside the theorem.
%   - Evaluation is limited to SWE-Bench-family benchmarks;
%     generalization to other issue distributions is untested.
%   - The method requires a pre-built code graph; its
%     construction cost and the sensitivity of results to graph
%     quality are not studied here.
%   - The coverage term is a differentiable surrogate, not the
%     exact (non-differentiable) set-coverage objective.
%   - Stop-timing and cross-round coordination are left to RL
%     and are not evaluated in this paper.
% ============================================================
